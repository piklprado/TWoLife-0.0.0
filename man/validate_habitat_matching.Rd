% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{validate_habitat_matching}
\alias{validate_habitat_matching}
\title{Validate Habitat Matching for Simulation Results}
\usage{
validate_habitat_matching(
  simulation_result,
  main = NULL,
  point_size = 2,
  landscape_colors = "terrain",
  show_stats = TRUE
)
}
\arguments{
\item{simulation_result}{A twolife_result object}

\item{main}{Character. Overall plot title. If NULL, auto-generates title
with survivor count (default: NULL)}

\item{point_size}{Numeric. Size of survivor points (default: 2)}

\item{landscape_colors}{Character. Color scheme: "habitat", "terrain", or
"viridis" (default: "terrain")}

\item{show_stats}{Logical. Print habitat quality statistics to console
(default: TRUE)}
}
\value{
Invisibly returns data frame with columns:
  \itemize{
    \item \code{id}: Individual ID
    \item \code{x, y}: Spatial coordinates
    \item \code{genotype}: Genotype value
    \item \code{habitat_value}: Habitat quality at individual's location
    \item \code{row_index, col_index}: Grid cell indices
  }
}
\description{
Creates a two-panel visualization showing the landscape and survivor positions
colored by genotype. Validates that individuals are in appropriate habitat
by checking the correlation between genotype and habitat quality at survivor
locations.
}
\details{
The function displays two side-by-side plots:
\itemize{
  \item Left: Landscape colored by habitat quality
  \item Right: Survivor positions colored by genotype
}
If habitat selection is working, dot colors should visually match the
background colors (individuals with high genotype values in high-quality
habitat, and vice versa).
}
\examples{
\dontrun{
result <- twolife_simulation(...)
validation_data <- validate_habitat_matching(result)
head(validation_data)
}

}
